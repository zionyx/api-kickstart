#! /usr/bin/python

client = EdgeGridClient()

# Search for existing same-name user, bail if found
# Create new user
# Search for new user (to get the user's info)
# Create new group
# Add user to group
# Remove user from group
# Delete group
# Delete user 
